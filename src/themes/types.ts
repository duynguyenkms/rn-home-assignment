export type AppColors = {
  primary: string;
  primaryLight: string;
  background: string;
  surface: string;
  text: {
    primary: string;
    secondary: string;
  };
  border: string;
  danger: string;
};
