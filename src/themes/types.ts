export type AppColors = {
  primary: string;
  background: string;
  surface: string;
  text: {
    primary: string;
    secondary: string;
  };
  border: string;
  danger: string;
  dangerLight: string;
  info: string;
  infoLight: string;
};
